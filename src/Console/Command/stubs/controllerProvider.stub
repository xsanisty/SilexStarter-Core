
            $app['{{controller}}'] = function (Container $app) {
                $controller = new \{{controller}}({{dependencies}});
                {{controllerAction}}
                return $controller;
            };
