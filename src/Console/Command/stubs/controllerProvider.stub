
            $app['{{controller}}'] = $app->share(
                function (Application $app) {
                    $controller = new \{{controller}}({{dependencies}});
                    {{controllerAction}}
                    return $controller;
                }
            );
